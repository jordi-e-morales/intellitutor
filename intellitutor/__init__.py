"""Minimal package to satisfy build backend for uv sync.

This project primarily runs as scripts; the package exists so that
the build backend (hatchling) can locate a package named
`intellitutor` matching the project name.
"""

__all__ = []

